# input ID!]!{
#   id: ID
# }

# Tag mutations
extend type Mutation {
  addUserToTag (
    name: String!
  ): Response @auth

  removeUserFromTag (
    name: String!
    userId: ID!
  ): Response @auth

  addTagsToList (
    id: ID!
    tagIds: [ID!]!
  ): Response @auth

  removeTagsFromList (
    id: ID!
    tagIds: [ID!]!
  ): Response @auth

  addTagsToNote (
    id: ID!
    tagIds: [ID!]!
  ): Response @auth

  removeTagsFromNote (
    id: ID!
    tagIds: [ID!]!
  ): Response @auth
}
