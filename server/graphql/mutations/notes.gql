extend type Mutation {
  createNote (
    title: String
    content: String!
  ): Response @auth

  modifyNote (
    id: ID!
    title: String
    content: String!
  ): Response @auth

  deleteNote (
    id: ID!
  ): Response @auth
}
