extend type Mutation {
  createNote (
    title: String
    content: String!
  ): Response

  modifyNote (
    id: ID!
    title: String
    content: String!
  ): Response

  deleteNote (
    id: ID!
  ): Response
}
