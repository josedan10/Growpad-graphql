input InputItems {
  name: String!
  checked: Boolean!
}

extend type Mutation {
  createList (title: String!): Response @auth

  deleteList (
    id: ID!
  ): Response @auth

  modifyList (
    id: ID!
    title: String
    input: [InputItems]
  ): Response @auth

}